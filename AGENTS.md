# 项目协作规则

## 仓库边界

- 可版本化的实际仓库根目录是 `E:\code\MAPPO\on-policy`，GitHub 远端为 `cspso`。
- `E:\code\MAPPO` 只是本机工作区容器；不能用它外层的空 Git 历史作为源码版本证据。
- 若 Codex 从 `E:\code\MAPPO` 启动，先切换到本仓库并读取本文件及下列研究文档。

## 项目目标
研究强化学习对 PSO 领袖选择的改进，并检验跨粒子、跨时间的干预信用估计。
研究定义和候选方案见 docs/SPEC.md；当前实现与验收状态见 docs/STATUS.md。
MAPPO、反事实基线和分支模拟本身不构成本项目已经证实的新颖性。

## 开始和恢复任务

1. 确认实际项目目录及适用的上级指令。
2. 读取 docs/STATUS.md、docs/VERIFICATION.md，以及 SPEC.md 中与任务有关的部分。
3. 检查 Git 提交、未提交差异和相关源文件；没有提交时明确标记版本未建立。
4. 用简短文字说明当前目标、已有证据和本次验收条件，然后执行任务。
5. 旧聊天和旧报告用于定位线索；当前结论需要核对当前代码或重新验证。
6. 当前研究文档的读取顺序为 `docs/STATUS.md`、`docs/VERIFICATION.md`、任务相关的 `docs/SPEC.md`，文献问题再读 `docs/REFERENCES.md`。

## 科学定义与实现
1. 以用户最新明确要求为准，记录与现有规格的变化。
2. 当前任务未涉及的奖励、动作空间、预算和干预含义，保持现有规格。
3. 在授权范围内改变科学定义时，同步记录原因、规格版本及受影响的实验。
4. 实现偏离规格时明确列出差异，不通过悄悄修改规格来消除差异。
5. 不用未来信息、测试集信息或已知最优值训练控制器，除非研究协议另有明确约定。
6. 所有真实目标函数调用均计入相应账目，训练主轨迹、干预和独立评估分别记录。

## 验证与证据
1. 区分：设计建议、已有代码、验证通过、结果支持、待复核。
2. 声称测试通过时附测试入口、代码版本、配置和结果路径；未运行明确写未运行。
3. 修复科学逻辑或训练错误时，优先建立能揭示该错误的最小验证，再运行相关回归。
4. 不为得到通过结果而无解释地放宽断言、删掉失败样本或改变统计口径。
5. 代码、配置或依赖改变后，检查既有证据的适用范围；不自动沿用原通过状态。
6. 文献结论引用实际查阅的来源；不凭文件名、摘要印象或旧聊天补造 DOI 和结论。
7. 不将短训练可运行等同于算法有效，也不将检索未发现等同于无人研究。
8. `episode_metrics.jsonl` 是逐回合原始指标，TensorBoard 用于观察曲线，汇总 CSV 用于检查和制表；三者用途不可混淆。
9. 正式实验开始前，必须确认 `docs/STATUS.md` 的实验门槛已满足，并使用独立实验名、明确种子和完整命令。
10. 未经用户明确要求，不删除正式实验原始日志。烟雾测试应使用明确的 `smoke_` 名称并在验收后清理。

## 结束与交接
1. 报告改动、验证结果、未验证事项和下一项具体任务。
2. 更新 STATUS.md；相关证据登记到 VERIFICATION.md，保留历史记录。
3. 已授权的常规实现和验证继续执行；只追问会实质影响目标的缺失信息。
