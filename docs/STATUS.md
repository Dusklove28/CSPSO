# 当前状态

最后更新：2026-09-17  
实际仓库：`E:\code\MAPPO\on-policy`  
远端：`cspso` -> `https://github.com/Dusklove28/CSPSO.git`  
本轮验收所依据的算法代码基线：`39bdd2d`
当前文档与代码版本：以包含本文件的 Git 提交为准，使用 `git rev-parse HEAD` 获取。
当前阶段：A1 工程验收通过，2026-09-17 主批次 80 个 Run 已完成并完成探索性分析。

## 已实现

- 环形邻域收缩系数 PSO，Actor 为每个粒子选择左、自己或右侧领袖。
- 共享参数 MAPPO、集中 Critic 和条件动作反事实 Critic。
- `mappo`、`cf_no_intervention`、`cf_intervention`、`cf_intervention_shuffled` 四种模式。
- 完整 PSO 快照、替代分支延续、配对环境与 Torch 随机状态。
- 主轨迹、干预、独立评估和总函数评估累计日志。
- 每回合 `episode_metrics.jsonl` 和 CSV 汇总入口。
- 标签历史池，使单样本稀疏干预也能执行跨批次错配对照。
- TensorBoard 使用单一 Run 下的普通 scalar 标签；旧实现把每个指标拆成独立 Run，已在当前工作单元修正。
- 训练入口支持 `--results_dir`，可将运行写入按日期组织的 `runs/<date>/PSO/...`。
- 每个训练 Run 自动生成 `run_manifest.json`，记录完整参数、命令、Git 状态、运行环境和完成/失败状态。
- 最后一个训练回合会强制执行评估，最终 checkpoint 有对应的独立评估指标。
- 评估间隔按已完成回合数判断，正式配置在第 10、20、...、200 回合评估，不在第 1、11、...回合评估。
- 正式命令直接使用当前 `cspso` PowerShell 调用训练入口，不依赖额外批处理脚本。

## 已有验证

- 空干预：替代动作等于实际动作时，配对终点差为 0。
- 负对照：`c2=0` 时更改领袖，终点差为 0。
- 合并预检：快照复现、分支恢复、成本对账、模型保存加载、Actor 更新、评估冻结和候选动作替换均通过。
- 正式主批次：80/80 Run 完成，版本、配置、评估点、最终原始评估值和函数评估账目检查均通过。
- 先前短运行已证明训练、评估和汇总链路可执行，但旧结果已按用户要求在正式实验前清理，不能作为保留的科学证据。

最新可复核命令和输出登记在 `docs/VERIFICATION.md` 与 `docs/evidence/`。

2026-09-17 首次正式启动在 Sphere/MAPPO/seed 1 期间发现评估间隔 off-by-one，用户在约第 31 回合停止。该未完成 Run 已移出正式结果树并归档到 `runs/_aborted/2026-09-17_sphere_mappo_seed1_run1_offbyone`，不得纳入汇总；修复后合并预检通过，正式结果尚未产生。

## 尚未验证

- 历史标签池对照的分布公平性。
- 独立、多次干预下的归因符号、排序和校准。
- 同总训练函数评估预算的 MAPPO 和无干预反事实补跑。
- 与环形最佳领袖 PSO、随机领袖规则在同一评估随机流上的比较。

## 2026-09-17 主批次结论

完整分析见 `docs/experiments/2026-09-17-primary-analysis.md`。真实干预监督相对无干预反事实为 9 胜、10 负、1 平，相对打乱标签为 11 胜、9 负；其平均训练函数评估比普通模式高 12.43%。当前数据不支持“真实干预标签带来稳定优化收益”，但反事实结构在 Rosenbrock 上出现明显信号。尚不能据此证明 RL 优于普通 PSO，也不能证明因果归因更准确。

## 2026-09-17 数据清理

用户授权清理正式实验前的旧数据。已停止占用日志的 TensorBoard 进程，并删除：

- `onpolicy/scripts/results/PSO` 下 317 个旧文件，约 5.8 MB；
- 根目录旧 `pso_summary.csv`。

这些数据包括烟雾测试、修复前检查和单种子 MAPPO 试跑，未纳入 Git，删除后不能从仓库恢复。它们不再参与后续汇总。

## 正式实验门槛

在以下条件满足前，不启动大规模正式性能实验：

1. `VERIFICATION.md` 中 V01、V04、V05、V06、V08、V09 与主批次相关的工程部分通过。
2. SPEC S12 的训练预算、评估协议和模型选择规则已由用户确认并冻结。
3. 每次运行保存代码版本、完整参数、种子、设备和起止状态的 manifest。
4. 四种学习模式使用相同任务配置；额外干预函数评估在主批次后按真实账目生成同成本补跑计划。

## 下一项任务

在查看进一步结果前冻结同成本取整、归因诊断和规则 PSO 评估协议；随后优先补做独立归因诊断，而不是直接扩大性能实验。
